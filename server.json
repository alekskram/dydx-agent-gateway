{
  "$schema": "https://raw.githubusercontent.com/modelcontextprotocol/registry/main/schema/registry-server.schema.json",
  "name": "io.ventures.dydx-agent-gateway",
  "description": "MCP gateway for AI agents to dYdX v4: market data, funding heatmap, technical analysis, verified trader analytics (deposit-adjusted PnL with reconciliation), leaderboards, anomaly detection (funding extremes, OI spikes, liquidation-cascade signatures, equity jumps) and optional EIP-712 trading with user-held keys.",
  "version": "0.2.1",
  "tools": [
    {"name": "market_digest", "description": "One-call briefing: events, funding extremes, leaderboard top"},
    {"name": "trader_pnl_stats", "description": "Deposit-adjusted PnL, day-winrate, maxDD, data-accuracy residual"},
    {"name": "liq_cascade_signature", "description": "via latest_events: liquidation cascade signatures from candles"}
  ],
  "packages": [
    {
      "registry_type": "pypi",
      "identifier": "dydx-agent-gateway",
      "version": "0.2.1"
    }
  ]
}
