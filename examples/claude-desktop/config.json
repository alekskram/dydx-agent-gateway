{
  "_comment": "Claude Desktop — local connection (stdio). Put this into claude_desktop_config.json (Settings -> Developer -> Edit Config). REPLACE path with your gateway folder.",
  "mcpServers": {
    "dydx": {
      "command": "/path/to/agent-gateway/.venv/bin/python",
      "args": ["-m", "dydx_mcp.server"],
      "cwd": "/path/to/agent-gateway",
      "env": {
        "PYTHONPATH": "/path/to/agent-gateway"
      }
    }
  }
}
