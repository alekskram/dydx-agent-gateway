{
  "_comment": "Claude Desktop — локальное подключение (stdio). Положить в claude_desktop_config.json (Settings -> Developer -> Edit Config). ЗАМЕНИ path на свою папку шлюза.",
  "mcpServers": {
    "dydx": {
      "command": "/path/to/agent-gateway/.venv/bin/python",
      "args": ["-m", "dydx_mcp.server"],
      "cwd": "/path/to/agent-gateway",
      "env": {
        "PYTHONPATH": "/path/to/agent-gateway"
      }
    }
  }
}
