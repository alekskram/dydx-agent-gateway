{
  "_comment": "Claude Desktop — connection to a hosted endpoint (streamable HTTP) when the gateway already runs somewhere as a service. No installation needed.",
  "mcpServers": {
    "dydx": {
      "type": "http",
      "url": "http://127.0.0.1:8901/mcp"
    }
  }
}
