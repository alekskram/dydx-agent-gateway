# dYdX Data-Quality Watchdog — Report #1

Период: 2026-08-24T06:26:19+00:00 · генератор: dydx-agent-gateway (авто)

## 1. Сверка тождества PnL (equity−Δ = Δpnl + ΣnetTransfers)

- Проверено аккаунтов (фондированных, из реестра): 25
- Остаток < $0.01: **25/25**
- Максимальный остаток: $0.0000
- Вывод: тождество выполняется на 100% выборки при корректной интерпретации
  netTransfers как по-бакетного потока.

## 2. Реестр адресов (блочный сканер)

- Адресов в реестре: 718 (растёт непрерывно, systemd dydx-scanner)
- Паттерн полноты: в сырых блоках встречаются обрезанные bech32-фрагменты
  (42 символа) — отфильтровываются валидатором длины (43).

## 3. Задокументированные особенности indexer API (грабли)

1. `netTransfers` в historical-pnl — поток за период, НЕ кумулятив.
   Наивная интерпретация даёт ошибку просадки:фактически 79.5% vs реальные 11.9%
   на эталонном маркет-мейкере (Δ 67 п.п. фантома).
2. `priceChange24H` в perpetualMarkets — не соответствует изменению цены
   за 24ч по свечам (проверено на ETH/BTC). Считать по свечам.
3. Per-fill PnL в /v4/fills отсутствует на мейннете — винрейт только
   через кривую historical-pnl.
4. Поле субтиков цены называется `subticksPerTick` (докам соответствовало
   бы subticksPerBase) — цена_сабтиков = price/tickSize*subticksPerTick.

## 4. Классификатор фармеров (эвристика v0)

- Пометок "likely rewards farmer": 0/25
- Эвристика: maker>=90% и avg fill<=$300 и >=50 филлов в выборке.

Следующий отчёт: через месяц или по триггеру новой находки.

## 5. Дополнение (2026-08-24, вечер): порядок свечей

- /v4/candles возвращает свечи **newest-first** (как historical-pnl).
  Потребитель, ожидающий хронологию, инвертирует знак ΔOI/Δцены и тренд TA.
  Нормализовано в api-слое шлюза (сортировка по startedAt).
- /v4/historicalFunding на мейннете отвечает 404 (путь проверяется).
